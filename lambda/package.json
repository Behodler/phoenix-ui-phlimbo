{
  "name": "faq-write-lambda",
  "version": "1.0.0",
  "description": "Authenticated Lambda that validates and writes faq-data.json to S3, then invalidates CloudFront.",
  "main": "index.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "rm -rf node_modules function.zip && npm install --omit=dev && npm run zip",
    "zip": "zip -r function.zip index.js package.json node_modules >/dev/null && echo 'Wrote function.zip'"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.700.0",
    "@aws-sdk/client-cloudfront": "^3.700.0",
    "@aws-sdk/client-secrets-manager": "^3.700.0"
  }
}
