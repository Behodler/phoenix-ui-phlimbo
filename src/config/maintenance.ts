/**
 * Maintenance Mode Configuration
 *
 * Set MAINTENANCE_MODE to true to display a full-screen maintenance banner
 * that blocks all interaction with the site. Set to false for normal operation.
 */
export const MAINTENANCE_MODE = true
